# 快速启动指南（更新版）

## 一、端口分配

| 服务 | 端口 | 访问地址 |
|------|------|----------|
| **用户端** | 5173 | http://localhost:5173 |
| **后台管理** | 5174 | http://localhost:5174/admin |
| **后端API** | 8888 | http://localhost:8888/api |

---

## 二、启动步骤

### 步骤1：启动数据库

```bash
docker start mysql-insurance
```

**验证**：
```bash
docker ps | grep mysql-insurance
```

---

### 步骤2：启动后端API（终端1）

```bash
cd backend
npm run dev
```

**验证**：
- 打开浏览器访问：http://localhost:8888/health
- 应该看到：`{"status":"ok","timestamp":"..."}`

---

### 步骤3：启动用户端（终端2）

```bash
cd /Users/cc/insuranceplateform
npm run dev
```

**访问**：http://localhost:5173

---

### 步骤4：启动后台管理（终端3）

```bash
cd admin
npm run dev
```

**访问**：http://localhost:5174/admin

---

## 三、验证关联

### 测试1：后台配置 → 用户端显示

1. **在后台管理系统配置产品**
   - 访问：http://localhost:5174/admin/products
   - 添加或查看产品

2. **在用户端查看产品**
   - 访问：http://localhost:5173/#/new-policy
   - 应该能看到后台配置的产品

### 测试2：后端API

```bash
# 测试健康检查
curl http://localhost:8888/health

# 测试产品列表
curl http://localhost:8888/api/products
```

---

## 四、常见问题

### 端口被占用？

```bash
# 检查端口
lsof -i :5173
lsof -i :5174
lsof -i :8888

# 杀死进程
kill -9 <PID>
```

### 前端无法连接后端？

1. 检查后端是否启动：`curl http://localhost:8888/health`
2. 检查浏览器控制台错误
3. 检查CORS配置（后端已配置）

---

**所有服务启动后，就可以看到三个系统的关联了！** 🎉

